You are writing the system prompt for a new north AI agent.

Agent name: {name}
Domain: {domain}
Description: {description}
Model pool: {model_pool}
Tools available: {tools}
Accepts task types: {accepts}

Write a concise but complete system prompt (200-400 words) that:
1. Defines the agent's role and expertise in the {domain} domain
2. Lists what kinds of tasks it handles
3. Describes its reasoning style and output format
4. Mentions the tools it can use

Output ONLY the system prompt text, no preamble.
