def moving_average(nums, k):
    """Return the averages of each consecutive length-k window of nums.

    E.g. moving_average([1, 2, 3, 4], 2) == [1.5, 2.5, 3.5]. Returns [] if k > len(nums).
    """
    raise NotImplementedError
